// use this file to define all my routes so I can change them later fr lol
//

// The name on import can be anything, because there’s only one default:
// lets export default, and can import without curly braces.
const routes = {
  home: "/",
  about: "/about",
  blurbify: "/blurbify",
  templates: "/templates",
};

export default routes;
